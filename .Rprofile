
if (grepl('4.1.1', R.version$version.string)) {
  .libPaths("U:/R/4.1.1")
} else if (grepl('4.0.2', R.version$version.string)) {
  .libPaths("U:/R/4.0.2")
}
# A fun quote to start us off
#fortunes::fortune()
